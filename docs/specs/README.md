# docs/specs/

Specifications this plugin implements. `state-machine.md` is authoritative:
the carrying artifact, its state field, the transition table, the rejection
rule for the gated transition, and the per-state refusals.
