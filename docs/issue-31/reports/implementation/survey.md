# Current-state survey — core canon reference transition

## This repo's current tree (`feasibility/`)

| File | Status vs core canon |
|---|---|
| `agents/warrant-hunter.md` | **Does not exist.** `grep -rl warrant` over this repo hits only `docs/` proposal/report prose, no `agents/` directory and no vendored file. Nothing to delete for item 1 — the standalone core `warrant` plugin (marketplace-registered, ships `warrant/agents/warrant-hunter.md` itself) is already the only warrant-hunter source this repo has ever had. |
| `feasibility/hooks/trailer-gate.sh` | Vendored copy, role-token-substituted (`feasibility:` prefix, `FEASIBILITY_CYCLE_OFF` kill switch). Logic is the same commit-trailer / one-subject-per-commit check as core's canon `core/hooks/trailer-gate.sh`, which now derives role identity from `CLAUDE_ROLE` at runtime and is registered globally (`core/hooks/hooks.json`, `PreToolUse` matcher `.*`). |
| `feasibility/hooks/record-fields-gate.sh` | Vendored copy with a real (non-skeleton) field set: what-was-done / why / upstream-basis, terminal-conditional next-steps + open-finding-resolution-path — same shape as core's canon copy. One divergence: this copy's `TERMINAL = {"verdict", "scope-approved"}` (`feasibility/hooks/record-fields-gate.sh:139`), not core's default `{"landed"}`. This is the per-role terminal-state divergence core issue #66's own record anticipated and built `RECORD_FIELDS_TERMINAL_STATES` for. |
| `feasibility/hooks/handbook-trigger-gate.sh` | Vendored copy, role-token-substituted only; same operational-surface heuristics and refusal shape as core's canon copy, registered globally. |
| `feasibility/hooks/directive.sh` | Hand-rolled boilerplate (trap, `FEASIBILITY_CYCLE_OFF` kill-switch case, `CLAUDE_ROLE` guard, opening/closing heredoc lines) wrapping role content. Unlike the data-engineering-rulebook precedent (4 short lines), this role's content is five full sections: YOU DECIDE, RESEARCH (phase 1, four probes), CURRENT-STATE SURVEY (phase 1), PROPOSAL (phase 1, verdict-selection rules), EXECUTION JUDGMENT (phase 2, quality bar), and a closing RECORD REQUIREMENTS paragraph. `core/hooks/lib/role-directive.sh`'s `core_role_directive` takes exactly four opaque string arguments (`you_decide use_when produces hand_off`), each echoed verbatim on its own line — the function does not interpret the labels, so any content can go in any slot, but five sections into four slots is a genuine fitting decision, not a mechanical 1:1 mapping (flagged for the approver in the proposal). |
| `feasibility/hooks/hooks.json` | Registers `directive.sh` (SessionStart) plus all three gates (PreToolUse) locally — identical shape to every other pre-migration rulebook. All three gates already fire globally from `core/hooks/hooks.json`; this file's `PreToolUse` block is exactly the redundant-registration drift `stub-check.sh` polices once the vendored scripts are deleted. |
| `feasibility/.claude-plugin/plugin.json` | Role identity only (name/description/author). No gate/agent references. No change needed. |

## Core canon state (read from the sibling `tokenmaxxxer-core` checkout, read-only)

- `core/hooks/hooks.json` — `PreToolUse` (matcher `.*`) already fires `trailer-gate.sh`, `record-fields-gate.sh`, `handbook-trigger-gate.sh` (plus `board-gate.sh`, `approval-gate.sh`, `gh-guard.sh`) for every session with the `core` plugin installed — installed at session/harness level (this session's own transcript already carries `core`/`scout`/`freelunch`/`terse` directives), not something this repo's `marketplace.json` declares.
- `core/hooks/lib/role-directive.sh` — `core_role_directive you_decide use_when produces hand_off`; reads `CLAUDE_ROLE`, per-role kill switch `<ROLE>_CYCLE_OFF` (bash-3.2-safe uppercasing via `tr`); auto-emits the closing `RECORD: docs/issue-<n>/reports/${role}.md, phase-gated per contract v3 s19` line.
- `core/hooks/record-fields-gate.sh` — reads `RECORD_FIELDS_TERMINAL_STATES` (space-separated `loop_state` values, default `landed`) per its own header (issue-66 survey: terminal-state divergence across rulebooks is genuine per-role semantics, kept as env-injected config rather than collapsed).
- `core/hooks/tests/stub-check.sh` — the drift-recurrence detector issue #31 item 5 asks this record to confirm passing: (a) no vendored `trailer-gate.sh`/`record-fields-gate.sh`/`handbook-trigger-gate.sh`/`parse-check.sh` under `hooks/` (maxdepth 3); (b) `directive.sh` structural check — must source `role-directive.sh`, call `core_role_directive`, and contain no other non-blank/non-comment line beyond plain `VAR=value` assignments.
- `docs/issue-66/reports/implementation.md` (core) — states the per-rulebook follow-up: delete vendored gate/`parse-check.sh` copies and their `hooks.json` entries; replace `directive.sh` with the lib-call stub; drop `stub-check.sh` in and run it; **"any rulebook whose `record-fields-gate.sh` copy relied on a non-`landed` terminal state sets `RECORD_FIELDS_TERMINAL_STATES` in its own `hooks.json` env for the gate, or in its `directive.sh`/session env, before deleting its local copy — otherwise it silently regresses to the `landed`-only default."** This repo is exactly that case (`{"verdict","scope-approved"}` != `{"landed"}`).
- `docs/issue-63/reports/implementation.md` (core) — confirms the vendored `agents/warrant-hunter.md` stub-conversion is the matching per-rulebook follow-up for the warrant side; not applicable here (no vendored copy exists).
- No documented mechanism in either core issue's record for *where* a rulebook's `RECORD_FIELDS_TERMINAL_STATES` override should live once its own `hooks.json` no longer registers `record-fields-gate.sh` at all (the gate now runs from core's own globally-registered entry, not this repo's). Neither `core/hooks/hooks.json` nor any rulebook's `hooks.json` in the sibling checkout shows an `"env"` key on a hook entry. Flagged as an open question in the proposal.

## Gap line

- Present in core, absent/placeholder in this repo: nothing structural — this repo's gate copies are already real (non-skeleton) implementations, unlike data-engineering-rulebook's placeholder ones. The gap is purely "still vendored, not yet pointed at canon."
- Present in this repo, redundant once canon fires: the three vendored gate scripts, this repo's `hooks.json` `PreToolUse` block, the hand-rolled `directive.sh` boilerplate.
- Role-unique and to be preserved: the five directive sections' actual content (YOU DECIDE / RESEARCH / CURRENT-STATE SURVEY / PROPOSAL / EXECUTION JUDGMENT / RECORD REQUIREMENTS text), `plugin.json`, and the `TERMINAL = {"verdict","scope-approved"}` behavior (via `RECORD_FIELDS_TERMINAL_STATES`, not a hardcoded copy).
- No vendored `agents/warrant-hunter.md` to remove (item 1 is a no-op for this repo, recorded explicitly rather than silently skipped).
